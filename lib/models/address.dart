class Address {
  int id_address;
  String complement;
  String zip_postal_code;
  String city;
  String country;

  Address(this.id_address, this.complement, this.zip_postal_code, this.city,
      this.country);
}
